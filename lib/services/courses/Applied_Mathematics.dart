import 'package:flutter/material.dart';

class AppliedMathematics extends StatelessWidget {
  const AppliedMathematics({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Applied Mathematics'),
      ),
      body: const Center(
        child: Text('Welcome to Applied Mathematics'),
      ),
    );
  }
}